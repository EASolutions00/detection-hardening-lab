# Pinned 2026-08-31, when the analysis core was written.
# Also recorded in docs/DECISIONS.md. A version bump partway through data
# collection makes earlier runs incomparable, so do not upgrade mid-experiment.
#
#   .venv/Scripts/python.exe -m pip install -r requirements.txt

numpy==2.5.2      # arrays and summary statistics
scipy==1.18.1     # chi-square, normal tail, Benjamini-Hochberg correction
pandas==3.0.5     # tabular output (not yet used by the core)
PyYAML==6.0.3     # rule parsing, for the dependency index (not yet written)
pytest==9.1.1     # tests

# Not needed. scipy.stats.false_discovery_control provides Benjamini-Hochberg,
# so statsmodels is not required. scikit-learn was listed in the original plan
# for Cohen's kappa, which belonged to T3 and is no longer in scope.
